# Stakeholder-path evidence

Create one folder per stakeholder or persona using the same ID as
`submission.yaml`, for example `evidence/real/RS-01/` or
`evidence/virtual/VS-01/`. Use descriptive filenames and point to each required
file from `submission.yaml`.

This README may remain in the final ZIP. Do not place secrets or unrelated
personal information in the submission.
