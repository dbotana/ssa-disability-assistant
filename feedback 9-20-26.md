feedback 9-20-26

user testing reported issues transcribing full SSN, the app only recorded the first 3 numbers. 

allow users to click "next" after every question to move on instead of clarifying "is that correct?" and waiting for the user to say "yes" or "no". this is to allow users with sight to move through the process faster. don't make the users say "yes" or "no" let them click the space bar to affirm yes, and only make them speak if it is incorrect to correct it.  

revise birth date question so that it allows the user to phrase the response as "month, date, year" instead of "year, month, date." then regenerate the audio for that question.  make all dates requested be in the format "month, date, year". 

when revising the form afterwards, allow the user to say phrases like "change conditions" or "add conditions" to update the form. currently when the user says "change condition" it just lets them replace conditions instead of adding new ones. all them to add a new condition. 